# OSV-Scanner configuration for the example app's Gemfile.lock.
# See https://google.github.io/osv-scanner/configuration/
#
# concurrent-ruby is pinned to `< 1.3.4` in ./Gemfile (see the comment there:
# newer versions break the CocoaPods/activesupport toolchain). The fixes for the
# advisories below first ship in 1.3.7, which is above that intentional pin. The gem
# is an iOS dev/build-time dependency only (CocoaPods) and is not part of the
# published npm package. Re-evaluate lifting the pin — and removing these entries —
# when the CocoaPods stack is confirmed compatible with concurrent-ruby >= 1.3.7.

[[IgnoredVulns]]
id = "GHSA-h8w8-99g7-qmvj"
reason = "concurrent-ruby@1.3.3 fix lands in 1.3.7, above the intentional `< 1.3.4` pin in Gemfile; iOS dev-only, not shipped. Re-evaluate 2026-07-02."

[[IgnoredVulns]]
id = "GHSA-6wx8-w4f5-wwcr"
reason = "concurrent-ruby@1.3.3 fix lands in 1.3.7, above the intentional `< 1.3.4` pin in Gemfile; iOS dev-only, not shipped. Re-evaluate 2026-07-02."

[[IgnoredVulns]]
id = "GHSA-wv3x-4vxv-whpp"
reason = "concurrent-ruby@1.3.3 fix lands in 1.3.7, above the intentional `< 1.3.4` pin in Gemfile; iOS dev-only, not shipped. Re-evaluate 2026-07-02."
